@* Code Owner: Jelena Cosic *@
@{
    ViewData["Title"] = "Dashboard";
    var name  = User.FindFirst(System.Security.Claims.ClaimTypes.Name)?.Value ?? "Admin";
    var email = User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value ?? "";
}

<div class="container" style="margin-top: 40px;">
    <h2>Hello, @name!</h2>
    <p>@email</p>
    <p>You'll soon be able to manage all system users here.</p>
    <a asp-controller="Mentor" asp-action="Index" class="btn btn-primary" style="margin-top: 16px;">Switch to Mentor View</a>
</div>